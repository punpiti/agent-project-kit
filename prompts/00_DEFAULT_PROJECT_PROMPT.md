# 00 — Default Project Prompt

Legacy compatibility entry point. Do not load this as an additional workflow.

```text
Read `.ai/PROJECT_STATE.md` and `.ai/agent-project-kit/STARTUP.md`.
Classify the requested deliverable, load exactly one primary route, and load at
most two secondary workflows whose triggers are actually present. If the task
is clear, proceed. Ask one outcome question only when materially ambiguous.
```
