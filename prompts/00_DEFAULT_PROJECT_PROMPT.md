# 00 — Default Project Prompt

Legacy compatibility entry point. Do not load this as an additional workflow.

```text
Read `.ai/PROJECT_STATE.md` and `.ai/agent-project-kit/STARTUP.md`.
Classify the requested deliverable and select exactly one primary pipeline.
Attach only the methods, lifecycle stages, quality gates, and state actions
whose triggers are actually present. Report any module omitted because of a
configured limit. If the task is clear, proceed. Ask one outcome question only
when materially ambiguous.
```
